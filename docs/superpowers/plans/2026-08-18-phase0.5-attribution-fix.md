# Agent Doctor — Phase 0.5 Attribution 修复计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 final review 抓出的 2 Critical + 2 Important：context attribution 在真实 DSH 落盘格式上是坏的（tool/result 字段路径错、totalTokens 虚报总量漏掉 messages），并统一 TruthLevel 类型、加强测试，让"context 为什么这么大"这条核心差异化真正站得住。

**Architecture:** 换用真实 DSH 落盘格式的 fixture（`advanced-toolchain`），修正 attribution 的字段路径以匹配真实格式（`tool/result` = `data.message.content[0].content` 嵌套、`user/message` = `data.content` 直接、`assistant/message` = `data.message.content`），新增 messages 归因，让 totalTokens 真正覆盖 system + messages + tool-result + tool-schema 四类。

**Tech Stack:** TypeScript + Node 22+，vitest（测试）。

## Global Constraints

- **唯一事实源**：只读，一切从 session log 重建。真实落盘格式是 `data.message`（已由用户拍板），不用 web 投影格式。
- **Truthfulness**：所有 token 归因都是 DERIVED estimate（用内容长度粗估），除非字段来自 DSH 直接报告的 fact（如 `usage.inputTokens`）。占位符（`{{...}}`）一律标 unknown，不给假数值。禁止伪造精度。
- **全英文**：源码注释、测试描述、commit message、README、demo 输出全英文（用户已拍板）。
- **数据驱动**：cordis 动词映射是数据表，不写死。
- **TruthLevel 单一来源**：`ContextContribution.level` 引用 `truth-level.ts` 导出的唯一 `TruthLevel`，不再三处分叉。

## 已核实的事实（计划依据，来自真实 fixture 实测）

- 真实 fixture `G:\deepseek-harness\examples\acp-agent\tests\snapshots\advanced-toolchain\session.jsonl`：
  - session 头 id = `11111111-1111-4111-8111-111111111111`，占位符 2 个：`{{cwd}}`（session 头）、`{{system}}` + `{{tools}}`（request/header）。
  - cordis 时序：seq 15 = `cordis_define`（args `{"plugin":{...},"name":"Snapshot Marker",...}`），seq 63 = `cordis_undefine`（args `{"pluginId":"snap-1"}`）。无 `cordis_run`，无 `cordis_inspect`。
  - `tool/result` data 结构：`{ turn, step, message, meta }`，`message.content[0].type === 'tool-result'`，真实文本在 `message.content[0].content[]`（内层数组）。
  - `assistant/message` data：`{ turn, step, message, usage }`，`usage = {inputTokens, outputTokens}`（mock 值 3/3），`message.content[]`。
  - `user/message` data 直接就是 message：`{ content[], source, role, id }`（无 `data.message` 嵌套）。
  - `request/header` data：`{ header: { config, system, tools }, reason }`，`tools` 是 **string** 类型（值 `{{tools}}`）。

---

### Task 1: 换真实 fixture + 补 cordis 动词 + 修解析器/动词测试

**Files:**
- Create: `test/fixtures/advanced-toolchain.jsonl`（从 DSH 复制并替换占位符）
- Modify: `src/cordis-verbs.ts`（补 `cordis_inspect_self` → `inspect`）
- Modify: `test/session-log.test.ts`（断言 id 和 cordis 动词名）
- Modify: `test/runtime-diff.test.ts`（classifyCordisCall 补 inspect_self 断言）

**Interfaces:**
- Consumes: 无新增
- Produces: `advanced-toolchain.jsonl` 合法 JSON，`classifyCordisCall('cordis_inspect_self') === 'inspect'`

- [ ] **Step 1: 复制真实 fixture 并替换占位符**

```bash
cp /g/deepseek-harness/examples/acp-agent/tests/snapshots/advanced-toolchain/session.jsonl /g/AgentDoctor/test/fixtures/advanced-toolchain.jsonl
cd /g/AgentDoctor
python - <<'PY'
p = 'test/fixtures/advanced-toolchain.jsonl'
text = open(p, encoding='utf-8').read()
# 三个都是带引号的字符串占位符，直接替换字符串值（保留引号）
text = text.replace('{{cwd}}', 'G:/AgentDoctor')
text = text.replace('{{system}}', '')     # -> 空字符串
text = text.replace('{{tools}}', '')      # tools 是 string 占位符，留空
open(p, 'w', encoding='utf-8').write(text)
PY
python -c "import json; [json.loads(l) for l in open('test/fixtures/advanced-toolchain.jsonl', encoding='utf-8')]"
```
Expected: 无异常，所有行合法 JSON，无 `{{` 残留。

- [ ] **Step 2: 写失败测试（session-log 用新 fixture id）**

把 `test/session-log.test.ts` 里所有 `cordis-tool-round.jsonl` 改成 `advanced-toolchain.jsonl`，并改两处断言：

```ts
  it('parses the first line as the session header', () => {
    const text = readFileSync('test/fixtures/advanced-toolchain.jsonl', 'utf-8')
    const parsed = parseSessionLog(text)
    expect(parsed.header.type).toBe('session')
    expect(parsed.header.id).toBe('11111111-1111-4111-8111-111111111111')
  })

  it('parses all events and preserves seq order', () => {
    const text = readFileSync('test/fixtures/advanced-toolchain.jsonl', 'utf-8')
    // ... 不变
  })

  it('extracts cordis tool/call events', () => {
    const text = readFileSync('test/fixtures/advanced-toolchain.jsonl', 'utf-8')
    const parsed = parseSessionLog(text)
    const cordisCalls = parsed.events.filter(e =>
      e.type === 'tool/call' && String(e.data.name).startsWith('cordis_'))
    expect(cordisCalls.length).toBeGreaterThan(0)
    expect(cordisCalls.map(c => c.data.name)).toContain('cordis_define')
  })
```

- [ ] **Step 3: 跑测试确认失败（id 不匹配）**

Run: `cd /g/AgentDoctor && npx vitest run test/session-log.test.ts`
Expected: FAIL — header.id 期望 `1111...` 但旧 fixture 是 `sess-0001`。

- [ ] **Step 4: 补 cordis 动词 + 测试**

在 `src/cordis-verbs.ts` 的 `CORDIS_VERB_MAP` 加一行：

```ts
export const CORDIS_VERB_MAP: Record<string, CordisVerb> = {
  cordis_inspect: 'inspect',
  cordis_inspect_self: 'inspect',   // NEW: real fixture uses this
  cordis_inspect_query: 'inspect',  // NEW: real fixture uses this
  cordis_define: 'define',
  cordis_mount: 'run',
  cordis_run: 'run',
  cordis_stop: 'stop',
  cordis_unmount: 'stop',
  cordis_undefine: 'undefine',
}
```

在 `test/runtime-diff.test.ts` 的 `classifyCordisCall` describe 里加断言：

```ts
  it('recognizes cordis_inspect_self and cordis_inspect_query as inspect', () => {
    expect(classifyCordisCall('cordis_inspect_self')).toBe('inspect')
    expect(classifyCordisCall('cordis_inspect_query')).toBe('inspect')
  })
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd /g/AgentDoctor && npx vitest run test/session-log.test.ts test/runtime-diff.test.ts`
Expected: session-log 3/3 过（换 fixture 后），runtime-diff 的 classifyCordisCall 4 个测试过；但 `runtime snapshot + diff` 两个测试会 FAIL（还用旧 fixture 的 cordis_mount 断言）——这是 Task 3 要修的，本任务不处理。

- [ ] **Step 6: Commit**

```bash
git add test/fixtures/advanced-toolchain.jsonl src/cordis-verbs.ts test/session-log.test.ts test/runtime-diff.test.ts
git commit -m "test: switch to real DSH on-disk fixture; add inspect_self/query verbs"
```

---

### Task 2: 统一 TruthLevel 类型

**Files:**
- Modify: `src/types.ts`（`ContextContribution.level` 引用 `TruthLevel`）
- Modify: `src/truth-level.ts`（删死代码 `Estimated`/`estimate`，只留 `TruthLevel`）

**Interfaces:**
- Produces: `types.ts` 的 `ContextContribution.level: TruthLevel`（含 `'hypothesis'`），`ToolVisibility.level` 保持窄类型 `'fact' | 'derived'`。

- [ ] **Step 1: 改 src/truth-level.ts（删死代码，只留类型）**

```ts
/** The four truth levels every derived number must be tagged with. */
export type TruthLevel = 'fact' | 'derived' | 'hypothesis' | 'unknown'
```

- [ ] **Step 2: 改 src/types.ts 引用 TruthLevel**

顶部加 import，`ContextContribution.level` 改类型：

```ts
import type { TruthLevel } from './truth-level.js'

/** One context contribution: how many tokens some component occupies. */
export interface ContextContribution {
  category: 'system' | 'messages' | 'tool-result' | 'tool-schema'
  tokens: number
  level: TruthLevel
  sourceId?: string
}
```

`ToolVisibility.level` 保持 `'fact' | 'derived'` 不变（它语义上只有这两级，用 TruthLevel 会不必要放宽）。

- [ ] **Step 3: 跑 typecheck 确认编译**

Run: `cd /g/AgentDoctor && npx tsc --noEmit`
Expected: 若 `context-attribution.ts` 里用了 `level: 'derived'`（是 TruthLevel 的子集），仍编译通过。

- [ ] **Step 4: Commit**

```bash
git add src/types.ts src/truth-level.ts
git commit -m "refactor: single-source TruthLevel type; drop dead estimate wrapper"
```

---

### Task 3: 修 runtime-diff 测试适配新 fixture

**Files:**
- Modify: `test/runtime-diff.test.ts`（`runtime snapshot + diff` describe 两个测试）

**Interfaces:**
- Consumes: `buildRuntimeSnapshots`、`diffRuntime`（不变）
- Produces: 测试断言新 fixture 的 define/undefine 时序

- [ ] **Step 1: 写失败测试（新 fixture 的 define → undefine）**

把 `runtime snapshot + diff` describe 改成：

```ts
describe('runtime snapshot + diff', () => {
  it('rebuilds at least two runtime snapshots from the session log', () => {
    const text = readFileSync('test/fixtures/advanced-toolchain.jsonl', 'utf-8')
    const snapshots = buildRuntimeSnapshots(parseSessionLog(text))
    expect(snapshots.length).toBeGreaterThanOrEqual(2)
    expect(snapshots[0].revision).toBeLessThan(snapshots[snapshots.length - 1].revision)
  })

  it('diffs the node added by cordis_define and removed by cordis_undefine', () => {
    const text = readFileSync('test/fixtures/advanced-toolchain.jsonl', 'utf-8')
    const snapshots = buildRuntimeSnapshots(parseSessionLog(text))
    // define (seq 15) adds a dynamic node, undefine (seq 63) removes it
    const addDiff = diffRuntime(snapshots[0], snapshots[1])
    expect(addDiff.added.length).toBeGreaterThan(0)
    expect(addDiff.added.some(n => n.origin === 'dynamic')).toBe(true)
    const lastDiff = diffRuntime(snapshots[snapshots.length - 2], snapshots[snapshots.length - 1])
    expect(lastDiff.removed.length).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 2: 跑测试确认失败/通过**

Run: `cd /g/AgentDoctor && npx vitest run test/runtime-diff.test.ts`
Expected: 先确认现有实现（define push 节点、undefine pop 节点）是否满足。若 `buildRuntimeSnapshots` 已正确处理 define/undefine（它把 define 当 push、undefine 当 pop），则直接 PASS；若否，报 FAIL 并需修 `runtime-snapshot.ts`。

- [ ] **Step 3: 若 FAIL，修 runtime-snapshot.ts 的 define 语义**

当前 `runtime-snapshot.ts` 的 `if (verb === 'run' || verb === 'define')` 已把 define 当 push，`else if (verb === 'stop' || verb === 'undefine')` 已把 undefine 当 pop。若测试 FAIL，检查 `cordis_define` 的 args 解析（`args.name` 是 `"Snapshot Marker"`，`args.code` 存在，`args.plugin` 也存在）是否正确产出了动态节点。预期当前实现能通过，无需改逻辑。

- [ ] **Step 4: Commit**

```bash
git add test/runtime-diff.test.ts
git commit -m "test: adapt runtime-diff tests to real define/undefine lifecycle"
```

---

### Task 4: 重写 context-attribution（真归因 messages + 正确字段 + truthfulness）

**Files:**
- Modify: `src/context-attribution.ts`
- Modify: `test/context-attribution.test.ts`

**Interfaces:**
- Consumes: `parseSessionLog`、`ContextSnapshot`、`ContextContribution`、`TruthLevel`
- Produces: `attributeContext(parsed): ContextSnapshot`，其中 contributions 覆盖四类（system/messages/tool-result/tool-schema），totalTokens = 四类之和，全部 level 正确标注。

- [ ] **Step 1: 写失败测试（断言四类 + 量级 + 占位符不假报）**

重写 `test/context-attribution.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import { parseSessionLog } from '../src/session-log.js'
import { attributeContext } from '../src/context-attribution.js'

describe('attributeContext', () => {
  const text = readFileSync('test/fixtures/advanced-toolchain.jsonl', 'utf-8')
  const snapshot = attributeContext(parseSessionLog(text))

  it('covers all four categories: system, messages, tool-result, tool-schema', () => {
    const cats = snapshot.contributions.map(c => c.category)
    expect(cats).toContain('system')
    expect(cats).toContain('messages')
    expect(cats).toContain('tool-result')
    expect(cats).toContain('tool-schema')
  })

  it('attributes real tool-result content (not ~1 token)', () => {
    const toolResult = snapshot.contributions.filter(c => c.category === 'tool-result')
    // the fixture's cordis_define/undefine results carry real text; their summed
    // estimate must exceed a trivial 1 token each
    const total = toolResult.reduce((s, c) => s + c.tokens, 0)
    expect(total).toBeGreaterThan(toolResult.length * 2)
  })

  it('attributes messages from user/message and assistant/message content', () => {
    const messages = snapshot.contributions.find(c => c.category === 'messages')
    expect(messages).toBeDefined()
    expect(messages!.tokens).toBeGreaterThan(0)
  })

  it('labels all contributions as derived estimates (no fake precision)', () => {
    for (const c of snapshot.contributions) {
      expect(c.level).toBe('derived')
    }
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /g/AgentDoctor && npx vitest run test/context-attribution.test.ts`
Expected: FAIL — 现有实现不产生 messages 类，tool-result 读错字段（`data.message` 不存在于旧 web fixture，但新 fixture 有 `data.message`），量级断言不满足。

- [ ] **Step 3: 重写 src/context-attribution.ts**

```ts
import type { ParsedSession } from './session-log.js'
import type { ContextSnapshot, ContextContribution } from './types.js'

/** Rough token estimate: content string length / 1.5, as a derived estimate. */
function roughTokens(text: string): number {
  return Math.round(text.length / 1.5)
}

/** Extract the plain-text content of a message content block array. */
function textOf(content: unknown): string {
  if (!Array.isArray(content)) return ''
  return content.map((block: any) => {
    if (typeof block?.text === 'string') return block.text
    if (block?.type === 'tool-result') return textOf(block.content)
    return ''
  }).join('\n')
}

/**
 * Attribute context composition from the real DSH on-disk format.
 * Categories: system (request/header.system), messages (user/message +
 * assistant/message content), tool-result (tool/result inner content),
 * tool-schema (request/header.tools).
 *
 * Truthfulness: all token figures are DERIVED estimates (content length),
 * because DSH's tokenMeter reports only a total, not a per-category breakdown.
 * Placeholder values ({{...}}) are skipped, never given a fake number.
 * See DESIGN.md truthfulness principle.
 */
export function attributeContext(parsed: ParsedSession): ContextSnapshot {
  const contributions: ContextContribution[] = []
  let total = 0

  const add = (category: ContextContribution['category'], tokens: number, sourceId?: string) => {
    if (tokens <= 0) return
    contributions.push({ category, tokens, level: 'derived', ...(sourceId ? { sourceId } : {}) })
    total += tokens
  }

  // system + tool-schema from the LAST request/header
  const header = parsed.events.filter(e => e.type === 'request/header').at(-1)
  if (header) {
    const h = (header.data as any).header
    if (typeof h?.system === 'string' && h.system.length > 0) {
      add('system', roughTokens(h.system))
    }
    if (typeof h?.tools === 'string' && h.tools.length > 0) {
      // tools is a JSON-encoded schema string in the real format
      add('tool-schema', roughTokens(h.tools))
    }
  }

  // messages + tool-result from surface events
  let messagesTokens = 0
  for (const e of parsed.events) {
    if (e.type === 'user/message') {
      // user/message data IS the message (data.content[])
      messagesTokens += roughTokens(textOf((e.data as any).content))
    } else if (e.type === 'assistant/message') {
      // assistant/message wraps the message in data.message
      messagesTokens += roughTokens(textOf((e.data as any).message?.content))
    } else if (e.type === 'tool/result') {
      // tool/result: data.message.content[0] is a tool-result block with inner content
      const msg = (e.data as any).message
      const inner = msg?.content?.[0]?.content
      add('tool-result', roughTokens(textOf(inner)), String(e.seq))
    }
  }
  add('messages', messagesTokens)

  return { totalTokens: total, contributions }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /g/AgentDoctor && npx vitest run test/context-attribution.test.ts`
Expected: PASS — 4 个测试全过。若 `tool-result` 量级断言仍 FAIL，检查真实 inner content 路径，必要时用 `python -c` 打印 `data.message.content[0].content` 确认。

- [ ] **Step 5: 跑全量测试 + typecheck**

Run: `cd /g/AgentDoctor && npx vitest run && npx tsc --noEmit`
Expected: 全部 PASS（session-log 3 + runtime-diff 5 + context-attribution 4），tsc 无错。

- [ ] **Step 6: 同步 demo.ts 输出（可选但推荐）**

`src/demo.ts` 的 context attribution 输出循环已经是通用的（遍历 contributions），无需改。但确认它现在会打印四类。无需改动即通过。

- [ ] **Step 7: Commit**

```bash
git add src/context-attribution.ts test/context-attribution.test.ts
git commit -m "fix: attribute real messages and tool-result from DSH on-disk format"
```

---

## Self-Review

**Spec coverage（对照 final review 发现）：**
- Critical #1（tool-result 读错字段）→ Task 1 换真实 fixture + Task 4 用 `data.message.content[0].content` 路径。✅
- Critical #2（totalTokens 虚报漏 messages）→ Task 4 新增 messages 归因，四类齐全。✅
- Important #3（TruthLevel 分叉 + 死代码）→ Task 2 统一到单一 `TruthLevel`，删 `estimate`/`Estimated`。✅
- Important #4（测试太弱）→ Task 4 断言量级 + 四类覆盖。✅

**Placeholder scan：** 无 TBD/TODO，所有代码含完整实现，字段路径全部来自实测的真实 fixture。

**Type consistency：** `ContextContribution.level: TruthLevel`（Task 2）被 Task 4 用 `level: 'derived'` 消费，`'derived'` 是 `TruthLevel` 子集，一致。`attributeContext` 返回 `ContextSnapshot`，签名未变，demo.ts 无需改。

**已知简化（Phase 0.5 记录）：**
1. `roughTokens` 仍是 chars/1.5 粗估——真实 token 需接 DSH tokenMeter，Phase 0 不接。
2. `request/header.tools` 真实值是 JSON-encoded string，Phase 0 直接对 string 估 token（含占位符已替换为空）。
3. `cordis_define` 语义上只是"定义不运行"，但 runtime-snapshot 把 define 和 run 都当 push 节点——这是 Phase 0 简化，见 DESIGN.md 已知简化。
